import { Translations } from "../types/Language";

type Keys = "ERROR";

export const commonLocaleStrings: Record<Keys, Translations> = {
  ERROR: {
    en: "An error has occurred",
    pt: "Ocorreu um erro",
  },
};
